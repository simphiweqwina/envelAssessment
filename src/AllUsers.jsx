import React from 'react';
import logo from './logo.svg';
import './App.css';
import { withAuthenticator, AmplifySignOut } from '@aws-amplify/ui-react';
class ListUsers extends Component {
    state = {}
    render() {
        return (  );
    }
}

export default withAuthenticator(ListUsers);